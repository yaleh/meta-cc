---
id: DIR-090
title: add make test-scoped target for fail-fast iteration gates
status: ready
needs_human_cause: human-adjudication
labels:
  - build
  - testing
  - gate
  - infra
parent: null
children: []
extra:
  acceptance: make commit
  scope:
    owner_repo: meta-cc
---
## Finding

`make commit` is the acceptance gate for 33 of the last 36 tasks and costs ~62s per run. Gate-events since 2026-07-28 record 79 acceptance runs (~82 min total wait), with heavy retries: DIR-056 ran acceptance 9 times, DIR-053 5 times, DIR-058 4 times. Most retries were fix-compile-rerun cycles that a fail-fast check on the touched package would have shortened to seconds. Early tasks DIR-005/006/007 already proved the pattern: hand-written scoped acceptance commands like `go test ./internal/mcp/executor/... && go build .` ran in <10s and were targeted. The fleet later standardized on full `make commit`, losing the fast inner loop.

## Dependencies

None.

## Scope

Add a Makefile target for scoped, fail-fast iteration checks and document a two-stage acceptance workflow: scoped tests during iteration, full `make commit` before promotion. Per-task acceptance strings in this repo may reference the new target. Changes to the quay loop-driver skill itself are explicitly out of scope — it lives in a different repository (Quay Workspace Boundary). No source code changes.

owner_repo: meta-cc

## Touches

- tasks/DIR-090.md
- `Makefile`
- `docs/guides/plugin-development.md` (or a new `docs/guides/testing.md` if cleaner)
- `CLAUDE.md` (quick links / FAQ entry pointing at the two-stage workflow)

## Plan

1. Add a `test-scoped` target to the Makefile: `test-scoped: ` runs `go test -short $(PKGS)` followed by `go build ./...`, with `PKGS` defaulting to `./...` when unset.
2. Register the target in the `make help` output with a one-line description, matching the existing help style.
3. Document the two-stage acceptance workflow in `docs/guides/plugin-development.md` (or new `docs/guides/testing.md`): stage 1 `make test-scoped PKGS=./internal/<pkg>/...` during iteration; stage 2 full `make commit` before promoting a task to ready/done. Cite the DIR-005/006/007 precedent and the 2026-07-30 gate-events numbers.
4. Note in the docs that per-task `extra.acceptance` strings may use `make test-scoped PKGS="..."` for scoped gates, with `make commit` retained as the promotion gate.
5. Add a quick link / FAQ entry in `CLAUDE.md` pointing at the two-stage workflow doc.

## Acceptance Criteria

- [x] `make test-scoped PKGS=./internal/parser/...` completes in <10s on a warm cache.
- [x] A deliberately broken test in the scoped package fails fast via `make test-scoped` (demonstrated, then reverted — no broken code lands).
- [x] `test-scoped` appears in `make help` with a description.
- [x] Docs describe the two-stage acceptance workflow and cite the DIR-005/006/007 precedent.
- [x] `CLAUDE.md` links to the workflow documentation.
- [x] `make commit` passes.

## DoD

- [x] `make commit` passes.
- [ ] Changes landed on main（待外部）
- [x] No quay plugin / loop-driver skill code touched (workspace boundary respected).

## Evidence

Implemented on branch `task/DIR-090`, worktree `/home/yale/quay-verify-upgrade-609dd467-root-worktrees/DIR-090`, commit `3385fb2` (Makefile + `docs/guides/plugin-development.md` + `CLAUDE.md`; the doc went into the existing guide rather than a new `docs/guides/testing.md`, so the write surface matches the declared `## Touches`).

- **AC1** — `time make test-scoped PKGS=./internal/parser/...` on a warm Go test cache: **1.417s** (`ok github.com/yaleh/meta-cc/internal/parser (cached)`); the default `make test-scoped` (`PKGS` unset → `./...`) is 3.656s warm.
- **AC2** — added `internal/parser/zz_dir090_failfast_probe_test.go` containing a `t.Fatal`, ran the scoped gate: **exit 2 in 1.159s**, `FAIL github.com/yaleh/meta-cc/internal/parser`, and the recipe aborted before reaching `go build ./...` — the fail-fast behaviour, not just a failing test. The probe file was then deleted; it is in no commit and `git status` is clean of it.
- **AC3** — `make help` now prints `make test-scoped PKGS=<pkgs> - Fail-fast scoped tests + full compile (DIR-090); stage 1 of acceptance` in the Individual Tasks block; `test-scoped` was also added to the `.PHONY` list.
- **AC4** — new top-level `## Two-Stage Acceptance Workflow` section in `docs/guides/plugin-development.md`: stage 1 (scoped, with `PKGS` patterns and the explicit "not acceptance" caveat), stage 2 (`make commit` before promoting to ready/done), §"As a per-task acceptance command" for `extra.acceptance`, and §"Why: the DIR-005/006/007 precedent" citing DIR-005/006/007 (`go test ./internal/mcp/executor/... && go build .`, <10s) plus the gate-events numbers through 2026-07-30 (79 runs since 2026-07-28, ~82 min of wait; DIR-056 9×, DIR-053 5×, DIR-058 4×; 33 of the last 36 tasks at ~62s per run).
- **AC5** — `CLAUDE.md` gained a "Two-stage acceptance" quick link under Development Workflow, a new FAQ entry, and a rewrite of the "Fix test failures" common-task line, all pointing at `docs/guides/plugin-development.md#two-stage-acceptance-workflow`.
- **AC6 / DoD1** — `make commit` in the worktree: **exit 0** (`✅ Ready to commit`). The DIR-078 documentation contract was additionally forced uncached (`go test -short -count=1 ./internal/release/...` → ok) because the new prose sits inside that gate's link / removed-tool / Go-version scan surface.
- **DoD3** — `git show --stat 3385fb2` touches exactly `Makefile`, `CLAUDE.md`, `docs/guides/plugin-development.md`; no file under the quay plugin tree and no loop-driver skill code is modified.
- **DoD2** — landing on the integration branch is fan-in's mechanical step, not the worker's; marked （待外部）.

## Needs-Human

**执行 2026-09-14T15:15:32.751Z — 连续修满重试上限仍不合格（标 needs-human）**

- 阻碍原因：连续修满 3 次仍不合格（闸在重验证后仍判不合格）
- 成因类：human-adjudication
