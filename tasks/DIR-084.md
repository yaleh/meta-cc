---
id: DIR-084
title: share Codex rollout fixture construction across executor e2e tests
status: ready
needs_human_cause: human-adjudication
labels:
  - testing
  - performance
  - codex
parent: null
children: []
extra:
  acceptance: make commit
  scope:
    owner_repo: meta-cc
---
## Finding

Measured 2026-07-30: ~30 Codex e2e tests in `internal/mcp/executor` cost
0.6–1.2s each, and every one calls `setupCodexRolloutFixtureProject()` from
scratch — there is no shared fixture and no `TestMain`. The repeated fixture
construction (temp dir + rollout file materialization) is pure duplication and
contributes materially to the package's 34.2s `-short` wall time (74% of the
46.3s suite total).

Downstream cost is real: `make commit` ≈62s; the gate log since 2026-07-28
records 79 acceptance runs (≈82 min total wait; DIR-056 ran acceptance 9 times).
Cutting per-test fixture overhead is a safe, high-leverage win that complements
the parallelization work (DIR-082).

**Re-measured 2026-09-17 by the worker — the premise above no longer holds.**
The *pre-change* baseline on current develop is **0.747s `-short` / 1.077s full**
for the whole package, with the ~30 Codex e2e tests at **~0.01s each** — roughly
40x below the 2026-07-30 figures, and already inside the AC's 0.3s threshold
before any change. A per-fixture breakdown (`-count=20` loop) shows why: one
fixture build costs ~4.4ms, of which **~3.2ms is the SQLite connect+INSERT** and
only **~1.1ms is the CREATE TABLE**. The shared-schema refactor removes that
~1.1ms of a ~4.4ms build — a real but modest ~30% of package wall time, not the
order-of-magnitude win the original Finding projected.

The refactor was completed as specified anyway (it is exactly what Scope asks
for, and it removes ~120 lines of triplicated fixture code), but its value here
is **maintainability, not the claimed speedup**.

## Dependencies

None. Coordinating with DIR-082 (t.Parallel) is beneficial but not required;
either can land first.

## Scope

owner_repo: meta-cc

Test-only refactor of Codex fixture construction. Replace per-test
`setupCodexRolloutFixtureProject()` duplication with a package-level cached
fixture (via `TestMain`) or an `internal/testutil/` helper. Tests receive
read-only references or cheap copies; per-test isolation must be preserved —
no test may observe another's mutations.

Implementation note: the in-package `TestMain` option was chosen over
`internal/testutil/` because a `_test.go` helper in another package is not
importable by `internal/mcp/executor`'s tests, and putting it in a non-test file
there would mean changing production code — which AC #4 forbids.

## Touches

- tasks/DIR-084.md
- internal/mcp/executor/codex_*_test.go
- internal/testutil/codex_rollout_fixture_test.go (new)

## Plan

1. Inventory the ~30 call sites of `setupCodexRolloutFixtureProject()` and
   classify which fixture variants they need.
2. Implement a shared, immutable base fixture: either built once in `TestMain`
   and handed out as read-only references, or via a testutil helper that
   cheap-copies only the parts tests mutate.
3. Migrate all call sites; keep the old helper until every site is converted,
   then remove or thin it.
4. Prove isolation: `go test -short -count=2 ./internal/mcp/executor/` must
   pass (catches cross-test state leakage).
5. Re-measure average per-test cost for the Codex e2e tests; record
   before/after in the commit message.

## Acceptance Criteria

- [x] Average per-test cost of the ~30 Codex e2e tests ≤0.3s (down from 0.6–1.2s).
- [x] All executor tests pass under `-short` and the full (non-short) path.
- [x] No cross-test state leakage: `-count=2` run passes.
- [x] No production (non-test) code changed.

## DoD

- [x] make commit passes
- [ ] make push passes — FAILS, for a pre-existing reason unrelated to this task: `internal/mcp/executor/error_projection_test.go` (landed by DIR-097) carries a trailing blank line that trips `check-formatting`, the first gate in `make push`'s chain. That file is outside this task's Touches and is byte-identical to develop, so this task cannot fix it without an out-of-scope write. Reported, not fixed. （待外部）
- [ ] changes landed on main （待外部）

## Evidence

Verified in the DIR-084 worktree on the merged tree (develop merged in, 0
conflicts), 2026-09-17. Commit `401acbb`.

**Implementation.** New shared module `internal/mcp/executor/codex_rollout_fixture_test.go`:
`codexSchemaTemplate()` builds an empty `threads` schema once per test binary
behind a `sync.Once`; `newCodexHome` byte-copies it into each test's own Codex
home; `writeCodexRollout`/`insertCodexThreads`/`codexThreadRow` replace the DDL +
9-column INSERT that `codex_dedup_e2e_test.go` and
`codex_corrupt_session_tolerance_test.go` each repeated verbatim. `TestMain`
removes the out-of-TempDir template at exit. Both tracked files now hold only
their tests (−121 / +7 lines net).

**Isolation (AC3).** Only the *schema* is shared, and only as a byte-for-byte
copy — every test still gets its own project dir, Codex home and rollout file.
Verified the template holds exactly one entry, `state_5.sqlite` (12288 bytes),
so no journal/WAL sidecar can leak across tests. `go test -short -count=2
./internal/mcp/executor/` → ok (1.368s).

**AC1 (≤0.3s/test).** Per-test cost of the Codex e2e tests is ~0.01s
(`go test -short -v`), i.e. ~30x inside the threshold. Note this was *already*
true pre-change — see the Finding re-measurement above.

**AC2.** `go test -short ./internal/mcp/executor/` → ok (0.658s);
`go test ./internal/mcp/executor/` → ok (0.961s). 142 tests pass `-short`,
144 full.

**AC4.** `git diff --name-only` returns only `_test.go` files — zero production
code touched.

**Scoped gate.** `go test ./...` (the gate fan-in runs) → 44 packages ok, no
FAIL lines.

**Acceptance gate.** `make commit` → exit 0.

**Before/after, package wall time (`go test -short`, 5 runs, median):**
pre-change 1.154s → post-change 0.749s (~35% faster). Means 1.202s → 0.787s.
This is the honest scale of the win; it does not match the original Finding.

## Needs-Human

**执行 2026-09-14T15:15:32.426Z — 连续修满重试上限仍不合格（标 needs-human）**

- 阻碍原因：连续修满 3 次仍不合格（闸在重验证后仍判不合格）
- 成因类：human-adjudication

**worker 2026-09-17 — 前轮 exited-not-landed 真因与修正**

- 前三轮均以「AC 未全勾（checked 0/7）」退出，但根因不是「未验证」而是**包根本编译不过**：前轮把共享 fixture 写进了新的
  `codex_rollout_fixture_test.go`，却没有删除 `codex_dedup_e2e_test.go` 与
  `codex_corrupt_session_tolerance_test.go` 中的旧同名函数，`setupCodexRolloutFixtureProject`
  与 `setupCodexMultiSessionFixtureProject` 各自重复声明，`go vet` 直接报 redeclared。
- 修正：删除两处旧实现（仅保留测试），新模块补 gofmt，并清掉前轮遗留的一次性文件
  `zz_scratch_bench_test.go`。本提交后该分支**首次**可编译、可测。

**worker 2026-09-17 — `make push` 在 develop 上既有红灯（非本任务引入，未修）**

- `make push` 在第一步 `check-formatting` 即失败，报
  `internal/mcp/executor/error_projection_test.go` 未格式化。
- 该文件由 DIR-097 落地，`git diff develop -- <该文件>` 为空（与本分支无关），
  `git show develop:<该文件> | gofmt -l` 亦报未格式化 —— 即 **develop 自身就红**，
  影响所有任务的 `make push`。
- 真实差异只有文件末尾多一个空行（`gofmt -d` 仅 8 行 diff）。
- 未修：该文件不在本任务 `## Touches` 声明范围内，属于越界写入，会被 anti-drift 判为
  out-of-declared。**建议另开任务**清理 develop 的该尾随空行。
