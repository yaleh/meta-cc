---
id: DIR-015
title: "DIR-015: it0 gate at todo->ready boundary to catch build failures early"
status: todo
labels:
  - milestone-candidate
parent: null
children: []
extra:
  dirStatus: proposed
  schema: v1
  acceptance: 'echo "DIR-015 acceptance: manual verification — it0 gate
    registration requires .quay/gates.yml modification and lifecycle_promote
    wiring"'
---
## Finding

3 consecutive `execute-milestone` runs in session `b2520f9f` returned `needs-human` with `it0-checks-failed`: DIR-003 (137K tokens), DIR-004 (128K tokens), DIR-001 (131K tokens). Total: ~396K tokens, 247 tool uses, ~6.5 min wasted. The `dod` gate passed todo->ready but the it0 check (build/test viability) only runs during execute, after promotion.

## Proposal

Register an `it0` gate that runs basic build/test verification at todo->ready promotion time, before the task reaches execute-milestone. This catches environment and dependency issues early.

## AC

- [ ] `it0` gate registered in `.quay/gates.yml`
- [ ] `lifecycle_promote` (todo->ready) runs it0 gate
- [ ] Gate fails if `go build ./...` or `go test ./...` fails
- [ ] Gate passes for buildable tasks

## DoD

- [ ] it0 gate implementation + registration
- [ ] Integration with lifecycle_promote
- [ ] Test: unbuildable task fails it0, doesn't reach ready
