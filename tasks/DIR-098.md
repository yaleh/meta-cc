---
id: DIR-098
title: surface malformed-file health in get_session_directory and
  inspect_session_files
status: done
needs_human_cause: human-adjudication
labels:
  - mcp
  - reliability
  - query
parent: null
children: []
extra:
  acceptance: make commit
  scope.owner_repo: meta-cc
---
## Finding

Dogfooding session (2026-07-30): a corrupt session file (`8eda8f4e-2c74-4176-ba6b-8c45e890df42.jsonl`, "no Claude entries" — empty/truncated) sat undetected in the project session directory until it hard-crashed `query_sessions` (twice) on 2026-07-30. `get_session_directory` and `inspect_session_files` do not surface empty/unparseable files as data — the failure stayed a surprise instead of being discoverable before any query ran.

Structural root cause: DIR-018 (done) fixed skip-and-report tolerance in the analysis `loadData` path, but that behavior was lost/not propagated when DIR-032/DIR-034 rewrote and re-wired the ListSessions path behind `query_sessions`. The discovery tools enumerate files without reporting their health, so consumers keep learning about corruption at crash time.

## Dependencies

Complements DIR-094 (sibling task, tolerance contract).

## Scope

owner_repo: meta-cc

Make file health visible as structured data in the two discovery tools, so malformed files are discoverable before they crash a downstream query. Parse errors become data, never exceptions. Healthy-corpus output must remain unchanged.

## Touches

- `tasks/DIR-098.md`
- `internal/provider/*.go`
- `internal/mcp/query/stage.go`
- `internal/mcp/query/*_test.go`
- `internal/mcp/executor/query_handlers.go`
- `internal/mcp/executor/*_test.go`

## Plan

1. Compute per-file health `{entries, parseable, empty, bytes, error?}` in a shared probe in `internal/provider/`.
2. `get_session_directory`: add a `malformed_files` aggregate (list of `{file, reason}`) to the response, empty for a healthy corpus.
3. `inspect_session_files`: report parse errors per file as structured fields instead of letting them propagate as crashes.
4. Add tests: an empty/truncated file is named as malformed with a reason; a healthy corpus produces an empty `malformed_files` list and output otherwise identical to before.

## Acceptance Criteria

- [x] On this machine's project corpus, both tools name the 8eda8f4e-style empty file as malformed WITH a reason string.
- [x] A healthy corpus produces an empty `malformed_files` list and otherwise unchanged output.
- [x] The finding is discoverable WITHOUT running `query_sessions`.
- [x] `inspect_session_files` returns structured parse-error data for a bad file instead of erroring.

## DoD

- [x] Health probe implemented and shared by both handlers.
- [x] Tests cover the empty-file case and the healthy-corpus case.
- [x] `make commit` passes.
- [ ] Changes landed on main（待外部）

## Verification

Recorded by the DIR-098 worker at commit `4614e21` (branch `task/DIR-098`), 6
files: `internal/provider/filehealth.go` + `_test.go` (new),
`internal/mcp/query/stage.go`, `internal/mcp/query/stage_malformed_test.go`
(new), `internal/mcp/query/stage_validation_test.go`,
`internal/mcp/executor/corpus_tolerance_gate_test.go`.

**Shape of the change.** One shared probe (`provider.ProbeFileHealth` /
`ProbeFilesHealth` / `MalformedFiles`) turns "is this file usable?" into data —
`{path, bytes, entries, parseable, empty, parse_errors?, error?, reason?}`, with
`reason` non-empty iff the file is malformed. It reads with the same line reader
(`parser.ReadLineFiltered`) and the same record definition as the query paths
(a well-formed JSON *object*, so the DIR-099 `wrong-shape` corruption — valid
JSON that is not a record — is caught, not just truncation), so a file the probe
calls parseable is one `engine.readJSONLFile` loads without error.
`get_session_directory` (claude **and** codex branches) gains an always-present
`malformed_files` list of `{file, reason}`; `inspect_session_files` gains
`malformed_files` plus per-file `file_health`, and no longer fails the whole
call for one bad path.

- **AC1** — verified twice, on the real corpus and mechanically.
  *Live*: seeded the exact 0-byte
  `~/.claude/projects/-home-yale-quay-verify-upgrade-609dd467-root/8eda8f4e-2c74-4176-ba6b-8c45e890df42.jsonl`
  into this machine's project corpus, called both handlers, and read back
  `malformed_files: [{"file": ".../8eda8f4e-2c74-4176-ba6b-8c45e890df42.jsonl",
  "reason": "file is empty (0 bytes, 0 records)"}]` from `get_session_directory`
  and the same file + reason (plus `file_health` `parseable:false, empty:true`)
  from `inspect_session_files`, with no error from either. The seeded file was
  removed afterwards (`ls` confirms it is gone). Honest caveat: the original
  2026-07-30 transcript is **no longer present** in the live corpus (`find`
  over `~/.claude/projects/` for `8eda8f4e*` returns nothing), so the live check
  re-created that exact file and deleted it rather than observing the original.
  *Mechanical*: the DIR-099 shared fixture installs an empty transcript under
  that very session ID, and the cross-tool tolerance gate now requires
  `get_session_directory` and `inspect_session_files` to name it in
  `malformed_files` with a non-empty reason (`mustName` + `malformedFileEntries`
  requires a reason on *every* entry, so "the ID appears somewhere" cannot pass
  without the reason).
- **AC2** — `TestHandleGetSessionDirectory_HealthyCorpusIsUnchanged` and
  `TestHandleInspectSessionFiles_HealthyCorpusIsUnchanged` pin it as a key set:
  the pre-existing keys/values are asserted individually and the full key list
  must be exactly the old set + the new keys (`malformed_files` for the
  directory; `malformed_files` + `file_health` for the inspector), so no other
  reshaping can hide in the diff. `malformed_files` is asserted non-nil, so a
  healthy corpus marshals `[]`, never `null` ("none found" vs "not reported").
  On the live corpus (50 files / 36 MB) the response is `malformed_files: []` —
  i.e. the probe produces **no false positives on 50 real transcripts**.
- **AC3** — `TestHandleGetSessionDirectory_HealthIsVisibleWithoutAnyQuery`
  names the corrupt file from the listing call alone; no query tool is invoked
  anywhere in this task's tests, and the tolerance gate drives the same
  assertion through the real executor ABI.
- **AC4** — `TestHandleInspectSessionFiles_ReportsBadFilesWithoutFailingTheCall`
  passes a good file, the 8eda8f4e-style empty file, a truncated file and a
  missing path in one call: no error; `malformed_files` names all three bad
  paths with reasons (`file is empty (0 bytes, 0 records)`, `invalid JSON at
  line 1 (1 of 1 records are not session objects)`, `cannot stat file: …`);
  `file_health` carries `parse_errors > 0` for the truncated file and the
  underlying error text for the missing one; the good file is still inspected at
  its full 2 records, while the healthy file is not flagged.
  *Deliberate contract change*: an unreadable path used to fail the whole call
  (taking every sibling's metadata with it and naming nothing — the DIR-018
  defect in a different tool). It is now a per-file health outcome, so
  `TestHandleInspectSessionFilesSuccessAndInspectionError` was updated to the
  new contract, with the old assertion's intent recorded as a comment at the
  site.
- **DoD1** — the probe is `internal/provider/filehealth.go`, called by
  `buildClaudeDirectoryResult` / `buildCodexDirectoryResult` (through
  `malformedFilesFor`) and by `inspectSessionFiles`; no tool has its own copy.
- **DoD2** — `internal/provider/filehealth_test.go` (probe unit tests, driven
  against the shared DIR-099 fixture: empty / truncated / wrong-shape, plus the
  control session as the false-positive guard) and
  `internal/mcp/query/stage_malformed_test.go` (both handlers, healthy + corrupt).
- **DoD3** — `make commit` → **exit 0** on the final code, after the last edits
  (`normalize-board-eof check-essential check-no-scanner test`).
- **Gate has teeth** — temporarily removing the two `malformed_files` keys made
  `TestCorpusToleranceGate/get_session_directory` go red
  (`the response must carry malformed_files`), so the strengthened gate is not
  vacuous; restored immediately and re-run green.
- Scoped gate `go test ./...` → **exit 0**; `anti-drift-touches-check --task
  DIR-098 --worktree <wt> --merge-target develop` → `ANTI-DRIFT OK: 6 actual
  file(s), all within declared Touches (6 glob(s))`; scoped-gate cache written
  (develop sha `1c976c4`). `git merge develop` → `Already up to date`, no
  conflicts.
- **Cost** (measured, not estimated): `get_session_directory` over this
  machine's live 50-file / 36 MB corpus = **~250 ms**, and the probe's `entries`
  agrees with the inspector's `line_count` on the real files it was compared
  against (327/327, 59/59, 506/506).
- **Scope hygiene**: `make commit`'s `normalize-board-eof` step appended
  trailing newlines to `tasks/AC118-00{1,2,3}.md` (outside this task's
  `## Touches`, pre-existing on develop — same side effect DIR-085 recorded).
  Reverted, not committed; the branch carries only the 6 files above.

## Needs-Human

**执行 2026-09-14T15:15:33.366Z — 连续修满重试上限仍不合格（标 needs-human）**

- 阻碍原因：连续修满 3 次仍不合格（闸在重验证后仍判不合格）
- 成因类：human-adjudication

**Resolution (2026-09-17, DIR-098 worker):** the recorded blocker was not a
quality failure. The only DIR-098 event in the gate-events log is a `retreat`
(`needs-human → todo`, actor `quay-cli`) whose reason attributes the verdict to
an expired Claude Code OAuth session — the workers could not start at all, so
worker-driver mechanically flipped the task to needs-human. No implementation
had ever been attempted. Re-dispatched with valid credentials, the change above
was implemented and verified on the first pass; the only test that had to move
was the one pinning the contract this task deliberately changes (AC4).
