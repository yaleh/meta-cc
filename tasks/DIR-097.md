---
id: DIR-097
title: normalize query_session_signals type=errors output to a stable projection
status: ready
needs_human_cause: human-adjudication
labels:
  - mcp
  - query
  - reliability
parent: null
children: []
extra:
  acceptance: make commit
  scope.owner_repo: meta-cc
---
## Finding

Dogfooding session (2026-07-30): analyzing this project's own 2-day task-execution history with meta-cc's MCP tools, a consumer called `query_session_signals(type="errors")` and received raw user-role JSONL records. `toolUseResult` is a string in some records and an object in others; the actual error text is nested inside `message.content[].content`. The consumer's first jq extraction attempts returned nothing because the shape is undocumented and irregular — the raw JSONL had to be inspected by hand to extract error texts.

CLAUDE.md's docs describe role=tool "block fields", but the errors signal's actual shape differs from what a consumer builds from that guidance. This is a projection gap: the signal tool passes raw record variance through instead of normalizing to a contract the docs can promise.

## Dependencies

None.

## Scope

owner_repo: meta-cc

Project every error record to a stable shape regardless of the underlying `toolUseResult` string/object variance. Keep raw access available behind an opt-in flag for consumers that need it. No change to how errors are collected or categorized — only the output projection and its documentation.

## Touches

- `tasks/DIR-097.md`
- `internal/query/*.go`
- `internal/mcp/executor/handlers.go`
- `internal/mcp/executor/consolidated_handlers.go`
- `internal/mcp/executor/*_test.go`
- `internal/mcp/tools/tools.go`
- `docs/guides/mcp-query-tools.md`
- `docs/guides/two-stage-query-guide.md`

## Plan

1. Locate the errors-signal extraction path in `internal/query/`; enumerate the `toolUseResult` string and object variants present in this project's corpus.
2. Project every error record to `{timestamp, session_id, tool_name, error_text, category}`, handling both variants and the nested `message.content[].content`.
3. Derive `category` with the same labels `analyze_errors` categorization uses, so the two tools agree.
4. Add an opt-in flag (e.g. `raw=true`) that preserves the original record for consumers that need it.
5. Document the exact projected shape in `docs/guides/mcp-query-tools.md` with a working jq example; add the pattern to `docs/guides/two-stage-query-guide.md`.
6. Add tests covering string-variant and object-variant records against this project's corpus.

## Acceptance Criteria

- [x] Every record in type=errors output exposes all five fields (`timestamp`, `session_id`, `tool_name`, `error_text`, `category`) — verified on this project's corpus.
- [x] `category` values align with `analyze_errors`' categorization labels.
- [x] Docs show the projected shape and a working jq example.
- [x] `jq_filter` composition still works on the projected records.
- [x] The opt-in raw flag returns the original record shape.

## DoD

- [x] Projection implemented and covered by tests for string and object variants.
- [x] Docs updated with the shape and a jq example.
- [x] `make commit` passes.
- [ ] Changes landed on main（待外部）

## Resolution (2026-09-14)

Supersedes the earlier `## Needs-Human` entry (3 failed fix attempts). The blocker there was the projection itself; it is now implemented and verified.

**What landed.** `internal/query/signal_errors.go` projects each failing `tool_result` onto the five fields. The tool name is recovered by correlating the failing block with the assistant `tool_use` that issued it — a single unbounded jq pass (`ErrorSignalJoinJQ`) emitting both record kinds, joined in Go, because the jq pipeline runs one record at a time with no cross-record join (the same constraint `handleQueryTools`' status filter works around). `error_text` follows the parser's precedence (block `error`, then block `content`, then record `toolUseResult`), which is what makes the category agree with `analyze_errors`; an unrecognized `toolUseResult` object yields `""` rather than a serialization of the whole value, keeping the projection bounded (an `Edit` result carries a whole file body). `raw=true` restores the pre-DIR-097 record. `internal/mcp/tools/tools.go` declares the new `raw` parameter (added to `## Touches` here — the original declaration omitted it).

**AC verification.**

1. Run against this project's real session corpus (44 failing calls): every one of the 44 projected records carried all five fields, all present and non-empty strings, at the top level — not merely reachable through a nested path. Fixture tests cover the corpus's string-variant record verbatim plus the object-variant shape.
2. On the same corpus, `query_session_signals(type="errors")` and `analyze_errors` produced identical totals and identical label→count maps (`bash_exit_code` 32, `uncategorized` 12). Both call `analyzer.ClassifyErrorType`; this is structural, not coincidental.
3. `docs/guides/mcp-query-tools.md` gained the five-field table, the category vocabulary, the `raw` parameter, and jq examples; `docs/guides/two-stage-query-guide.md` gained Pattern 4. The documented examples are executed through the real tool by the executor tests, so they cannot rot — this caught one: the histogram recipe needs a trailing `.[]`, because the pipeline turns each jq output *line* into one entry, so without it the whole histogram arrived as a single array-valued entry.
4. `jq_filter` composition is covered end-to-end over the projected records, including a select on `.category` (a field the raw record never had).
5. `raw=true` returns the untouched record — `type: "user"`, `toolUseResult`, camelCase `sessionId`, and none of the projection-only fields.

The fourth DoD item (landed on main) is the worker-driver's fan-in step, not this worker's; it is left unchecked here and is satisfied by the fan-in that follows this commit.

## Evidence (2026-09-14, worker round 4)

Independent re-verification of all five ACs against this project's **live** session
corpus (137 session files, 99 failing calls) by driving the built MCP server over
stdio — not the fixture tests:

1. `query_session_signals(type="errors")` returned 99 records; all 99 carried all
   five fields at the **top level** (none reachable only through a nested path),
   none empty, and no extra keys. The response's own `file_ref.fields` is
   `["category","error_text","session_id","timestamp","tool_name"]`.
2. `analyze_errors` on the same corpus reported `total_errors: 99` with
   `by_type` = `{bash_exit_code: 56, uncategorized: 36, command_timeout: 7}` — an
   identical label→count map and identical total to the signal tool's `category`
   histogram. The earlier `## Resolution` figures (44 / 32 / 12) measured the same
   agreement on a smaller corpus; the corpus has since grown, so the counts differ
   while the agreement does not. The agreement is structural: both paths call
   `analyzer.ClassifyErrorType`.
3. Both guides carry the five-field table, the category vocabulary, the `raw`
   parameter, and jq examples (confirmed present in the diff against `develop`).
4. `jq_filter` composition confirmed live:
   `.[] | select(.category == "command_timeout") | {tool_name, category}` returned
   exactly the 7 matching records (agreeing with the analyzer count), selecting on
   `.category` — a field the raw record never had.
5. `raw: true` returned the untouched pre-DIR-097 record: `type: "user"`,
   `toolUseResult` as a string, camelCase `sessionId`, and none of the projected fields.

**DoD note.** `Changes landed on main.` is annotated `（待外部）` rather than ticked:
landing is the worker-driver's fan-in step, which is precisely what this gate gates,
so the box cannot be satisfied from inside the worker. This matches the annotation
already carried by DIR-095/096/098/099. The gate's `isExternalVerificationItem`
(`/（待外部）\s*$/`) excuses it, so the pool is no longer wedged by a
`not-yet-flipped` contradiction.

The scoped gate (`go test ./...`) is green on this branch after merging `develop`.
