---
id: DIR-025
title: "DIR-025: establish one release version and MCP capability source of truth"
status: done
labels:
  - release
  - metadata
  - reliability
parent: null
children: []
extra:
  acceptance: make commit
---
## Finding

meta-cc currently publishes mutually inconsistent capability metadata:

- `.claude-plugin/marketplace.json` reports version `3.4.0` and advertises 22 MCP tools.
- `plugin-src/.codex-plugin/plugin.json` reports version `3.0.7`.
- the MCP startup banner reports server version `1.0.0`.
- the live `tools/list` response and server tests expose 15 tools.
- `CLAUDE.md` and several project documents still advertise 21 tools, while the public README uses 15.

This is not merely cosmetic. Agents use the tool count to decide whether capabilities are missing, package managers use versions to decide whether an update is available, and release verification cannot distinguish a correctly assembled bundle from a stale one.

## Proposal

Create one machine-readable release/capability source of truth and generate or verify every published version and tool-count surface from it.

### Contract

- The server version, Claude marketplace version, Codex plugin version, release archive metadata, and documentation release version must agree.
- The advertised MCP tool count must be derived from the actual registered tool definitions, not maintained as an independent magic number.
- CI and local release checks must fail with a useful diff when a version or tool-count surface drifts.
- Historical proposal/plan statements may remain as historical records, but current guides, manifests, banners, and examples must be corrected.

### Design constraints

Prefer a small checked-in metadata file plus a deterministic verification/generation script over parsing prose. Avoid silently rewriting manifests during ordinary builds. The check must run without network access and must distinguish current product documentation from historical design documents. Do not change the tool set solely to make an old count true.

### Alternatives considered

- Manually editing all numbers fixes today's snapshot but guarantees recurrence.
- Treating each package format as independently versioned is unsupported by the current release process and confuses users.
- Counting documentation occurrences with a broad grep would incorrectly flag historical documents.

## Plan

1. Inventory all current version and tool-count surfaces, categorize them as authoritative, generated/verified, historical, or test fixtures, and add failing consistency tests.
2. Introduce the smallest machine-readable release metadata source and a script or Go helper that compares it with plugin manifests, the compiled server version, and the actual `GetToolDefinitions()` count.
3. Wire the consistency check into `make check-release-ready`, plugin-sync checks, and CI without requiring network access.
4. Correct current manifests, startup/version output, README/CLAUDE guidance, installation docs, and MCP reference pages. Clearly mark historical counts where needed rather than rewriting history.
5. Add a contributor/release note explaining how a version bump and tool addition are performed and verified.
6. Run the focused consistency checks, package tests, release smoke checks, and repository commit gate.

## Acceptance Criteria

- [ ] One documented machine-readable source defines the current release version.
- [ ] Every shipped Claude/Codex/server version surface agrees with that source.
- [ ] The current advertised tool count equals the real `tools/list` count.
- [ ] A deliberately stale manifest or current-doc count makes the consistency test fail with an actionable message.
- [ ] Historical proposal and plan records are not treated as current product claims.

## Definition of Done

- [ ] Offline consistency verification is part of normal pre-release CI.
- [ ] Current user-facing documentation contains no contradictory version or tool-count claims.
- [ ] `make commit` passes.

## needs-human: adversarial audit found REFUTATION (incomplete current-doc coverage)

Build completed in worktree `milestones/meta-cc/DIR-025` (branch of the same name, branched off `main` at commit `a0c94bd`, merged/rebased not yet needed since it's already current). Implementation summary: added `internal/version/release.json` + `internal/version/version.go` (go:embed'd single source of truth for the release version, consumed by `cmd/mcp-server/main.go`/`server.go` instead of hardcoded `"1.0.0"`), `internal/release/consistency_test.go` (offline tests asserting all 4 plugin manifests agree with `release.json`, and a curated list of current docs/manifests match the real `len(tools.GetToolDefinitions())` count), corrected the drifted surfaces (Codex plugin → 3.4.0, marketplace/plugin descriptions → 15 tools, CLAUDE.md/DOCUMENTATION_MAP.md/integration.md → 15 tools, rewrote `docs/guides/mcp.md`'s stale tool catalog), wired the check into `make check-release-ready`, `scripts/sync-plugin-files.sh --verify`, `scripts/release/pre-release-check.sh`, and `.github/workflows/release.yml`, and updated the version-bump scripts to keep the Codex plugin + release.json in sync going forward.

I independently verified the gate myself (not just the build subagent's self-report): `make commit` passes cleanly in the worktree with `PATH` corrected for `go`/`goimports` (this environment's `gate_run` MCP tool subprocess doesn't inherit `/usr/local/go/bin` from `~/.bashrc`, a known unrelated infra quirk — see DIR-024's notes).

A **fresh-context adversarial audit found a REFUTATION** before landing, which blocks this task per the loop-driver's audit policy:

**Defect**: `docs/reference/features.md:20` states verbatim **"meta-cc exposes 21 MCP tools"** (and lists individually-named legacy tools like `query_tools`, `query_tool_errors` that no longer exist, consolidated away in an earlier change). This is a current, non-historical reference doc — it is not under any of the excluded directories (`docs/proposals/`, `docs/plans/`, `docs/phases/`, `docs/archive/`, `plans/`, `tasks/`) that `internal/release/consistency_test.go` itself documents as historical. The real tool count is 15. The diff did not touch this file, and the new `currentToolCountDocs` allowlist in `consistency_test.go` (the mechanism used to decide which current docs get checked) only covers 8 hand-picked files — README.md, CLAUDE.md, docs/DOCUMENTATION_MAP.md, docs/guides/integration.md, docs/guides/mcp.md, and the 3 manifest JSONs — `docs/reference/features.md` is absent from it. `TestAdvertisedToolCountMatchesRegisteredTools` passes despite this live contradiction, because the check is a curated allowlist rather than a genuine current-vs-historical directory classifier, so it silently under-covers the true current-doc set. This directly contradicts the task's own AC ("current advertised tool count equals the real count") and DoD ("current user-facing documentation contains no contradictory ... claims").

The auditor verified everything else is sound: the version single-source-of-truth mechanism is genuine (drift-detection re-confirmed by deliberately breaking `release.json` to `9.9.9`, seeing an actionable multi-surface failure, then restoring it exactly); the tool-count check is a real derivation (`len(tools.GetToolDefinitions())`), not a second hardcoded constant; CI wiring is correctly exercised via `go test ./...` in `ci.yml` and explicitly in `release.yml`; `make commit` passes.

**Required before landing**: either (a) broaden the current-doc detection from a hand-picked allowlist to something that actually walks all current (non-excluded-directory) markdown for tool-count/version claims, or (b) at minimum add `docs/reference/features.md` (and audit for any other current doc in the same situation) to the allowlist and fix its stale "21 MCP tools" / legacy tool-name listing to match the real 15.

Diff remains uncommitted in worktree `milestones/meta-cc/DIR-025` (branch of the same name) for whoever picks this up.
