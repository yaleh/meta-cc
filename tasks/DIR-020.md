---
id: DIR-020
title: "DIR-020: Extract isAllNullOrEmpty into shared utility package"
status: todo
labels:
  - milestone-candidate
parent: null
children: []
extra: {}
---
## Finding

`isAllNullOrEmpty` duplicated in `internal/query/engine/stage2.go:79-105` and `internal/mcp/query/query.go:391-414`. Already diverged: stage2.go returns jq errors properly, query.go silently discards them.

## Proposal

Create `internal/query/common/types.go` with shared function. Both callers import from common. Remove duplicated definitions.

## AC

- [ ] `isAllNullOrEmpty` defined in exactly one location
- [ ] Both callers import from shared location
- [ ] Identical behavior for all edge cases

## DoD

- [ ] Duplication removed
- [ ] Tests added
- [ ] `make commit` passes
