---
id: DIR-086
title: eliminate triple full-test-run in make push
status: todo
labels:
  - build
  - testing
  - performance
parent: null
children: []
extra:
  acceptance: make commit
  scope:
    owner_repo: meta-cc
---
## Finding

Measured 2026-07-30: the Makefile `push` target currently runs the full Go test suite THREE times: `test` (short mode, inside `test-all`), a bare `go test -v ./...` (inside `test-all`), and `test-coverage-check` (`go test -coverpkg=./... -coverprofile=... ./...`). The documented `make push` budget is "<120s" but measured reality is far above it. Context: 79 acceptance gate runs in 2 days (≈82 min total), and 33/36 recent tasks use `make commit` as acceptance — push-tier waste compounds across the fleet.

## Dependencies

None

## Scope

Restructure `test-all` / `test-coverage-check` so the full (non-short) suite runs exactly ONCE with `-coverpkg`/`-coverprofile` (tests + coverage in a single pass), with E2E targets running around it. Delete the redundant invocations. Keep the 80% coverage threshold check operating on that single profile. This is scheduling optimization, not coverage relaxation — every existing gate must survive.

owner_repo: meta-cc

## Touches

- Makefile

## Plan

1. Trace the current `make push` execution (`make -n push` plus a timed run) and record the three `go test` invocations as baseline evidence.
2. Consolidate the full suite into a single invocation that produces the coverage profile in one pass.
3. Order E2E targets around that single run and delete the redundant short/bare invocations.
4. Re-point the 80% coverage threshold check at the single profile and verify it still fails a below-threshold run.
5. Measure the new `make push` wall time and attach before/after timings to the task.

## Acceptance Criteria

- [ ] `make push` invokes `go test` over the full package list at most once (evidence: command trace attached to the task).
- [ ] Coverage gate still fails below 80% (negative run demonstrated).
- [ ] `make push` wall time ≤150s measured.
- [ ] E2E targets still execute in the push path.

## DoD

- make commit passes
- changes landed on main
