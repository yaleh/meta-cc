---
id: DIR-093
title: extend dispatch-prompt gates to eliminate recurring tool-misuse round-trips
status: todo
labels:
  - infra
  - reliability
  - directive
parent: null
children: []
extra:
  acceptance: make commit
  scope:
    owner_repo: meta-cc
---
## Finding

meta-cc analyze_errors over 2026-07-25..07-30 shows recurring agent
tool-misuse round-trips (~13 wasted round-trips in 5 days):

- Read of nonexistent file ×6 (signature c10431437d701636) — the single
  most recurrent error pattern.
- Edit/Write before Read ×3 (signature fa9c208b72b8ee6c).
- Invalid empty `pages` parameter on Read ×4 (signature 85c85a050441de2f).

DIR-010 already landed a "read-before-edit gate for subagent dispatch
prompts". This task EXTENDS that gate; it does not duplicate it.

## Dependencies

DIR-010 (read-before-edit gate) must already be landed. Locate its artifacts
via `git log --grep=DIR-010` and tasks/DIR-010.md before editing.

## Scope

owner_repo: meta-cc

Only this repository's dispatch-prompt gate text and docs are modified.

## Touches

- dispatch-prompt gate files DIR-010 landed (locate via
  `git log --grep=DIR-010` / tasks/DIR-010.md)
- docs/guides/plugin-development.md

## Plan

1. Read DIR-010's landed gate text to understand the existing
   read-before-edit rule; extend it rather than forking a new gate.
2. Add coverage for nonexistent-file Reads (6 recurrences): dispatch prompts
   should embed verified file paths; when unsure, agents must glob/grep to
   confirm a path before Read.
3. Add coverage for empty optional parameters (4 recurrences): never pass
   empty-string values to optional parameters such as Read's `pages`; omit
   the parameter entirely instead.
4. Keep the existing read-before-edit rule (3 recurrences) intact.
5. For each of the three classes, include one concrete bad/good example.
6. Add a measurable review step: re-check recurrence via
   meta-cc analyze_errors over the trailing 7 days.
7. Update docs/guides/plugin-development.md to reference the extended gate.

## Acceptance Criteria

- [ ] Gate/prompt text covers all three misuse classes (nonexistent Read,
      Edit/Write-before-Read, empty optional parameter) with concrete
      bad/good examples.
- [ ] Existing DIR-010 read-before-edit rule is preserved (extended, not
      duplicated).
- [ ] Task body / gate text records baseline counts: 6 / 3 / 4.
- [ ] A follow-up measurement procedure is documented: run analyze_errors
      over the trailing 7 days, expecting 0 recurrences.
- [ ] `make commit` passes.

## DoD

- make commit passes
- changes landed on main
