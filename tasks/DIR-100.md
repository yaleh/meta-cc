---
id: DIR-100
title: fail-closed release validation — fix dead pre-release-check.sh path and
  bound the release test run
status: ready
needs_human_cause: human-adjudication
labels:
  - milestone-candidate
  - build
  - infra
  - release
  - gate
parent: null
children: []
extra:
  acceptance: make commit
  scope:
    owner_repo: meta-cc
---
## Finding

`scripts/release/release.sh` Step 1 pre-release validation is dead code in the current tree, and the release path's test phase is unbounded. Together this produced a recurring release-failure/timeout chain in the loop driver.

Structural defects:

1. **Path mismatch → validation permanently skipped (fail-open).** `release.sh` guards with `[ -f "scripts/pre-release-check.sh" ]`, but the script actually lives at `scripts/release/pre-release-check.sh`; `scripts/pre-release-check.sh` does not exist (verified on current tree: `ls: cannot access 'scripts/pre-release-check.sh': No such file or directory`). Every release run therefore takes the fallback branch and prints `⚠️  Warning: pre-release-check.sh not found (skipping validation)` — real pre-release validation never runs.
2. **Warn-and-continue masks the breakage** — the fallback degrades to minimal "Basic checks" and proceeds silently instead of failing closed.
3. **Unbounded test phase** — the fallback's `Run tests...` phase runs the full verbose suite with no timeout, so the loop driver's Bash watchdog kills a half-finished release at 10 minutes.

Evidence — session `2895bdb9-1e32-4af8-9906-73db13b7a584` (2026-07-30), three chained events:

- `06:26:59Z`: `Exit code 2 / Creating release v3.7.0... / Step 1: Running pre-release validation... / ⚠️  Warning: pre-release-check.sh not found (skipping validation) / Basic checks: / Error: Working directory not clean. Commit or stash changes. / make: *** [Makefile:214: release] Error 1`
- `06:27:16Z`: same `pre-release-check.sh not found (skipping validation)` warning, retry proceeds into `Basic checks: Running tests...`
- `06:37:26Z`: `Exit code 143 / Command timed out after 10m 0s / [main d4cd60f] chore: fix formatting in consistency test ... Creating release v3.7.0... / Step 1: Runni...` — release killed mid-run, left half-finished.

The analyze_bugs fix-pair analyzer independently flags this shape as recurring (error signature `77af6e2589fbfb5d`: fix_count=2, recurrences=2), with examples quoting both the `pre-release-check.sh not found (skipping validation)` + `Working directory not clean` failure and the skip-then-tests path.

This contradicts the documented contract that repo validation targets complete within bounded time (CLAUDE.md: `make push` full check "<120s"); a release should not consume a 10-minute watchdog budget.

## Acceptance Criteria

- [x] `release.sh` resolves the validator at its real path (`scripts/release/pre-release-check.sh`, resolved relative to the script/repo root, not `$PWD`); a guarded/dry `make release` run demonstrably reaches the real validator — no `pre-release-check.sh not found (skipping validation)` output.
- [x] Missing/unresolvable validator fails closed (non-zero exit with an actionable message); the warn-and-skip fallback is removed or gated behind an explicit `--skip-checks`.
- [x] Fast preconditions (branch, clean tree) run before any long-running phase — a dirty-tree release fails in seconds, as the 06:26:59 event should have without first entering validation.
- [x] The release test phase is bounded (`-short` mode and/or explicit timeout) with the bound documented, so a full release completes well under the loop-driver watchdog.
- [x] `make commit` passes.

## Touches

- `tasks/DIR-100.md`
- `scripts/release/release.sh`
- `Makefile`

## DoD

- [x] Path fix + fail-closed validation landed.
- [x] Bounded release test phase documented and verified.
- [x] `make commit` passes.
- [x] Changes landed on main. （待外部）

## Evidence

Verified in the DIR-100 worktree on the merged tree (develop merged in, 0 conflicts), 2026-09-17:

- **AC1** — `make release VERSION=v9.9.9 DRY_RUN=1` reached the real validator (Checks 1–6 emitted, incl. `✓ Working directory is clean` and `✓ Unit tests pass (short mode)`); `grep -c 'not found (skipping validation)'` = **0**. It then failed closed on an intentional version mismatch (target 9.9.9 vs tree 3.8.4) in 21s — the correct new behaviour, and proof validation is no longer skipped.
- **AC2** — with the validator genuinely absent (clean throwaway repo so Step 0 passes): exit 1, `❌ ERROR: pre-release validation script not found — refusing to release (fail-closed)`, naming the resolved path and the explicit `--skip-checks` escape hatch. No warn-and-skip, no unbounded `make all` fallback.
- **AC3** — dirty tree, invoked from an unrelated `$PWD` (/tmp): exit 1 in **0s**, `Release aborted before validation (no long-running phase was entered)`. Also demonstrates `REPO_ROOT` is resolved from the script, not `$PWD`.
- **AC4** — `RELEASE_VALIDATION_TIMEOUT=2` killed the validator at its bound (`exceeded its 2s bound and was killed`, elapsed 2s); `=abc` and `=0` are both rejected, so the bound cannot be silently disabled. A full validator pass measured 21s, well under the default 300s and the 600s watchdog.
- **AC5** — `make commit` exit 0; scoped gate `go test ./...` green across all packages.

## Needs-Human

**执行 2026-09-14T15:15:33.576Z — 连续修满重试上限仍不合格（标 needs-human）**

- 阻碍原因：连续修满 3 次仍不合格（闸在重验证后仍判不合格）
- 成因类：human-adjudication
