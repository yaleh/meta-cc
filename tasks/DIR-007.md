---
id: DIR-007
title: "BUG: BuildStatsFirstResponse silently swallows stats generation errors"
status: done
labels:
  - milestone-candidate
  - defect
parent: null
children: []
extra:
  schema: v1
  acceptance: "export PATH=/usr/local/go/bin:$PATH && cd
    /tmp/meta-cc-worktrees/DIR-007 && go test ./internal/mcp/pipeline/... && go
    build ./... && echo 'PASS: DIR-007 tests + build'"
---
## Finding

In `internal/mcp/pipeline/pipeline.go:247-254`, `BuildStatsFirstResponse` calls `querypkg.GenerateStats` / `GenerateTimestampStats` / `GenerateSessionStats` but discards the error with `_`. Compare with `BuildStatsOnlyResponse` (lines 204-210 of the same file) which properly checks and returns errors. If stats generation fails, the response has an empty or malformed stats section with no error indication. The caller sees a "success" response with missing data.

## Proposal

Capture and return errors from stats generation functions in `BuildStatsFirstResponse`, consistent with the pattern already used in `BuildStatsOnlyResponse`. Log the error with `slog.Error` (matching the existing logging pattern at lines 240-245) and return an empty string with the error.

## Plan

1. Fix `internal/mcp/pipeline/pipeline.go:248-255` — replace `stats, _ = querypkg.Generate*Stats(jsonlData)` with proper error capture: `var err error; stats, err = querypkg.Generate*Stats(jsonlData); if err != nil { slog.Error(...); return "", err }`
2. Add test case for `stats_first` mode when stats generation fails — verify error is propagated, not silently swallowed

## Acceptance Criteria

- [x] `BuildStatsFirstResponse` returns errors from stats generation
- [x] Consistent error handling with `BuildStatsOnlyResponse`

## Definition of Done

- [ ] Error propagation fixed
- [ ] Test added
- [ ] `make commit` passes

## Reconciliation Note (DIR-047, 2026-07-28)

Left as-is (not fixed under DIR-047 — out of scope for that task). The
`stats, _ = ...` pattern this task describes is confirmed still present
verbatim on `main` today at `internal/mcp/pipeline/pipeline.go:385-390`
(`BuildStatsFirstResponse`), and this task's milestone branch
(`milestones/meta-cc/DIR-007`, commit `863231c`) was likewise never merged —
the same unmerged-branch pattern as DIR-005/006. However, DIR-047's
investigation found this is currently a dead-code smell rather than a live
bug: all three callees (`GenerateSessionStats`, `GenerateTimestampStats`,
`GenerateStats`) always return a nil error today, so the swallowed `_` never
actually discards a real error in practice. Acknowledged but intentionally
not fixed here; a future task should either fix the error propagation for
defense-in-depth or fix `status` to reflect that the code fix was never
actually landed.
