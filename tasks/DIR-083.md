---
id: DIR-083
title: skip FTS/Unicode parity tests (9.6s) in short mode
status: ready
needs_human_cause: human-adjudication
labels:
  - testing
  - performance
parent: null
children: []
extra:
  acceptance: make commit
  scope:
    owner_repo: meta-cc
---
## Finding

Measured 2026-07-30: the two slowest individual tests in the suite are
dual-scan parity comparisons, both in
`internal/mcp/executor/fts_content_e2e_test.go`:
`TestQuerySessionContent_FTSParityWithCanonicalScan` (5.3s) and
`TestQuerySessionContent_UnicodeCaseFoldingParity` (4.3s) — together 9.6s of
serial wall time on every short run. They exist to prove the FTS/Unicode fast
path agrees with the canonical scan; that guarantee matters on the full path
(`make push` / CI), not on every `make commit` cycle.

Context: `go test -short -count=1 ./...` takes 46.3s total (executor alone
34.2s, 74%). `make commit` ≈62s; since 2026-07-28 there have been 79 acceptance
gate runs (≈82 min total wait, DIR-056 alone ran acceptance 9 times), so every
second removed from `-short` compounds across the whole gate history.

## Dependencies

None.

## Scope

owner_repo: meta-cc

Test-only change: gate both parity tests behind `testing.Short()` with an
explanatory skip comment. The tests must remain fully wired into the non-short
path used by `make push` and CI so the parity guarantee is not lost, only
relocated off the hot path.

## Touches

- tasks/DIR-083.md
- internal/mcp/executor/fts_content_e2e_test.go

## Plan

1. Add `if testing.Short() { t.Skip(...) }` at the top of
   `TestQuerySessionContent_FTSParityWithCanonicalScan` (5.3s) and
   `TestQuerySessionContent_UnicodeCaseFoldingParity` (4.3s), each with a
   comment explaining: dual-scan parity is expensive and remains enforced on
   the full `make push` / CI path.
2. Verify both still execute non-short:
   `go test -run 'FTSParityWithCanonicalScan|UnicodeCaseFoldingParity' ./internal/mcp/executor/`.
3. Re-measure `-short` suite wall time; record before/after vs the 46.3s
   baseline in the commit message.

## Acceptance Criteria

- [ ] `-short` suite wall time reduced ≥9s vs the 46.3s baseline.
- [x] Both parity tests still execute (not skipped) under `go test ./internal/mcp/executor/` (non-short).
- [x] Skip reason documented in a comment at each skip site, referencing the full-path guarantee.
- [x] make commit passes.

## DoD

- [x] make commit passes
- [x] make push passes with both parity tests visibly running
- [ ] changes landed on main（待外部）

## Evidence

Verification round 2026-09-14, all commands run in this task's worktree
(`.worktrees/DIR-083`, branch `task/DIR-083`).

**AC1 is left unticked: its premise is stale and the criterion is unachievable
as written.**

- Direct cost of the two named tests, measured here: **0.22s, not 9.6s**.
  `go test -count=1 -v -run 'FTSParityWithCanonicalScan|UnicodeCaseFoldingParity' ./internal/mcp/executor/`
  → `--- PASS: TestQuerySessionContent_UnicodeCaseFoldingParity (0.18s)`,
  `--- PASS: TestQuerySessionContent_FTSParityWithCanonicalScan (0.22s)`.
- Whole `-short` suite here: ~6.7-7.5s warm (17.5s on the first, cold run that
  built every test binary), against the Finding's recorded 46.3s.
- Root cause of the divergence: cf6efaf (2026-08-21, "pin files backend in codex
  fixture helpers") made `setupCodexRolloutFixtureProject` set
  `META_CC_CODEX_BACKEND=files`
  (`internal/mcp/executor/codex_dedup_e2e_test.go:27`). Before that pin these two
  tests drove the codex provider in auto mode, spawning a real `codex app-server`
  child per query — that was the measured 5.3s + 4.3s. The Finding was measured
  2026-07-30, three weeks *before* that fix landed.
- So "reduced ≥9s" cannot be met: there is no 9s left to remove. Skipping 0.22s of
  work cannot move a suite by ≥9s, and the ~39s of headroom between 46.3s and
  today's ~7s belongs to cf6efaf, not to this change. The delta this change
  actually produces (0.22s) is below run-to-run noise on this host — alternating
  A/B on the executor package alone swung 2.34-5.57s per run.
- AC1 needs human amendment (e.g. to "the two named tests no longer run under
  `-short`, with the measured delta recorded") — or the task closed as
  already-fixed by cf6efaf, if a 0.22s skip is not judged worth the lost `-short`
  coverage. Deciding that is a manager/human call, so the box is left unticked
  rather than re-read into passing.

**Ticked criteria, with their evidence:**

- AC2 — both parity tests still execute (not skipped) non-short: both `--- PASS`
  as above, 0.18s + 0.22s; re-verified after merging develop.
- AC3 — each `t.Skip()` site carries a comment naming dual-scan parity as the
  cost and pointing at the full path that still enforces it. `make push` runs
  `go test` *without* `-short` (Makefile `test-all`), so the guarantee is
  relocated, never dropped.
- AC4 — `make commit` exit 0 ("✅ Ready to commit"; workspace, fixtures, deps,
  short-mode tests all green).
- DoD "make push passes with both parity tests visibly running" — exit 0,
  coverage 85.9%, bats + lint + build green, and the verbose `test-all` log
  contains `--- PASS: TestQuerySessionContent_UnicodeCaseFoldingParity (0.22s)`
  and `--- PASS: TestQuerySessionContent_FTSParityWithCanonicalScan (0.23s)`.
- Scoped gate `go test ./...` (non-short, the same gate fan-in runs) — exit 0 on
  a tree containing develop@bf4b20f; scoped-gate cache written for that sha.
- DoD "changes landed on main" left unticked with `（待外部）`: landing is the
  outer layer's step, not the worker's.

**Two incidental defects found — reported, and only the in-scope one touched:**

1. `## Touches` declared the test file on a bare line with no `- ` bullet. The
   Touches parser is bullet-only (`anti-drift-touches-check.js`:
   `line.match(/^[-*]\s+(.+)$/)`), so the very file the task's own `## Scope`
   names explicitly was invisible to the touches gates. The bullet was restored
   here to match the Scope section — no write surface was added beyond what the
   Scope already declared — and the change is disclosed for review.
2. `anti-drift-touches-check --task DIR-083 --worktree ... --merge-target develop`
   exits 3 with `BASELINE-MISMATCH: merge target 'develop' is not a continuation
   of the project's default branch 'main'`. The gate itself labels this "a
   BASELINE defect, not an out-of-declared write by the task" and prescribes
   `quay init --force --adopt-branch-model`. That is a workspace-wide mutation
   far outside this task's scope and was deliberately **not** performed. It is
   systemic, not specific to DIR-083: the identical message reproduces against a
   control run on `.worktrees/DIR-089`. (DIR-094 flipped done via fan-in
   shortly before, so the driver's own invocation may resolve the baseline
   differently — flagged for a human, not diagnosed further here.)

## Needs-Human

**执行 2026-09-14T15:15:32.324Z — 连续修满重试上限仍不合格（标 needs-human）**

- 阻碍原因：连续修满 3 次仍不合格（闸在重验证后仍判不合格）
- 成因类：human-adjudication
